# Repository Instructions

- Jetson 远端测试统一使用 `ssh nvidia@192.168.1.230`，相关 benchmark 和验证都以这台机器为准。
- 没有 CUDA 的机器不要跑本地推理测试；本地 benchmark 仅允许在 CUDA 设备上执行。
- 生成音频后必须使用 `http://192.168.1.230:10001` 的 ASR 服务自动校对；优先使用 OpenAI 兼容接口 `POST /v1/audio/transcriptions`，文档入口是 `http://192.168.1.230:10001/docs`。
- ASR 返回中若包含 `<|xxx|>` 之类模型标签，必须先清洗，再参与文本比对和验收。
- 需要访问代理时，优先使用 `http://192.168.1.133:10807`，并尽量通过环境变量传递，避免把代理配置写死在代码里。
- Docker 构建要尽量避免重复长时间构建：优先复用已有镜像和层缓存，只同步必要文件，并尽可能控制镜像体积。
- 涉及 vLLM 长文本生成的改动，必须自动完成测试，并根据生成结果和 ASR 校对结果自动验收；修复未完成前不要宣布结束。
- 当发现单次生成存在长度限制时，优先通过稳定的工程方案规避，例如长文本分段生成与结果拼接，而不是只依赖手工调参。
- 听书场景的默认测试基线使用句级切分，优先按句子拆分输入；默认用 `256` 字长度进行验证。
- vLLM benchmark 需要分别统计启动时间与请求时间；请求性能仅从服务 ready 后开始计算，启动时间单独记录。
- 每一次 benchmark 都要把 `result.json`、逐候选 `candidate_result.json`、`report.md` 和生成音频保留在对应输出目录中。
- 需要评估不同并发量下的速度表现时，优先通过句子拆分后的批量并发方式测试，并输出各并发量的耗时、音频时长与实时倍率。
